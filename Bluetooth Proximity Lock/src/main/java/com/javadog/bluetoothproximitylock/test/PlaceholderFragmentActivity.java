package com.javadog.bluetoothproximitylock.test;

import android.os.Bundle;
import android.support.v4.app.FragmentActivity;

import com.javadog.bluetoothproximitylock.R;

/**
 * Used for tests. Simply holds a single fragment.
 */
public class PlaceholderFragmentActivity extends FragmentActivity {
	@Override
	protected void onCreate(Bundle savedInstanceState) {
		super.onCreate(savedInstanceState);
		setContentView(R.layout.placeholder_fragment_activity);
	}
}
